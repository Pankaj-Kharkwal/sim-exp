// Workflow diff store stub
import { create } from 'zustand'

interface WorkflowDiffStore {
  // Add store state and methods here when implementing
}

export const useWorkflowDiffStore = create<WorkflowDiffStore>()((set) => ({}))
